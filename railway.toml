[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && npm run build && npx prisma migrate resolve --rolled-back 20260502170000_add_argentina_to_country; npx prisma migrate deploy"

[deploy]
startCommand = "cd backend && node dist/index.js"
